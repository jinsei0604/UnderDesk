# V27 制作メモ

最新実機動画では、V26の振りかぶり後に剣先が左後方へ残り、体をまたいでから右へ出るため、振り下ろし自体が背中側に見えていました。

V27の新規5セルは、真上の頂点、後方への準備、頭上を越えた右上、体の右前を通る縦斬り、右下への踏み込み、の順です。その後は既存の低いフォロースルーへ接続します。

新規セルのソース列順は0、1、2、3、4です。全セルを同一倍率0.38461538で縮小し、足元ソースYを214へ固定し、既存12セルの色へ量子化しています。

剣座標はフレーム7だけが後方準備です。フレーム8〜10では剣先Xが柄Xより大きく、振り下ろし中に後方へ戻りません。

カットイン素材はV27のassetsへ含めず、Godot参照にもCUTIN定数、preload、関数、引数、タイマー、シグナルを残していません。

Claude Code用プロンプトにはテスト禁止を最上位条件として記載しています。Claude Codeはファイル配置と既存実装への反映だけを行い、実行確認はしません。
